import { StackNavigationProp } from '@react-navigation/stack';
import { ScreenList } from '@types';

/**
 * 네이버 지도 테스트 스크린 프롭
 * 네비게이션 관련 프롭들을 포함한 네이버 지도 테스트 스크린 옵션
 */
export type NMapTestScreenProps = StackNavigationProp<ScreenList, 'NMapTestScreen'>;

/**
 * 네이버 지도 테스트 스크린 옵션
 */
export type NMapTestScreenInterface = undefined;
